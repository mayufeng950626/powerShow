数据库是项目开发中的数据存储和处理平台
mysql的数据库集群分为PXC分片和RP（re'pi'li'ca）分片，mysql在单秒插入数据超过2000万的时候，数据库效率会急剧下降
PXC集群是同步传输数据（每个mysql节点必须同步数据，每个节点都处理完数据，事务才算成功）
RP集群是异步传输数据（只要有一个节点处理完数据，事务才算成功，不适合保存高一致性的数据）


在单节点中可以使用主键自动生成，但是在多节点中不能使用主键自动生成，防止主键冲突











